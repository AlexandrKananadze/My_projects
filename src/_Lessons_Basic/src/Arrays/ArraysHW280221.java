package Arrays;

import java.util.Scanner;

public class ArraysHW280221 {
   // public static void main(String[] args) {
   //     Scanner console = new Scanner(System.in);
   //     System.out.println("Введите размер массива");
   //     int c = console.nextInt();
   //     int b = console.nextInt();
   //     int[][] a = new int[c][b];
   //     for (int i = 0; i < c; i++) {
   //         System.out.println(" ");
   //         for (int j = 0; j < b; j++) {
   //             a[i][j] = (int) (Math.random() * 10);
   //             System.out.printf("%5d ", a[i][j]);
   //         }
   //     }
   //     System.out.println();
   //     // сумма под главной сумма под побочной, макс главной, макс побочной
   //     int sumpodglav = 0;
   //     int sumpodpoboch = 0;
   //     int num1 = Integer.MIN_VALUE;
   //     int num2 = Integer.MIN_VALUE;
   //     for (int i = 0; i < a.length; i++) {
   //         for (int j = 0; j < a.length; j++) {
   //             if (i > j) {
   //                 sumpodglav = sumpodglav + a[i][j];
   ////                 if (a[i][j] > num1) {
   ////                     num1 = a[i][j];
   ////                 }
   //             }
   //             if (j > a.length - 1 - i) {
   //                sumpodpoboch = a[i][j] + sumpodpoboch;
  ////                 if (a[i][j] > num2) {
  ////                     num2 = a[i][j];
  ////                  }
   //             }
   //         }
//
//
   //     }
   //     System.out.println(sumpodglav);
   //     System.out.println(sumpodpoboch);
   //     countunik (a);
   // }
//
   // public static int asd(int[][] a) {
   //     int count1 = 0;
   //     for (int i = 0; i < a.length; i++) {
   //         for (int j = 0; j < a.length; j++) {
   //             if (i > j && i > a.length - 1 - j) {
   //                 count1++;
   //             }
//
   //         }
   //     }
   //     int[] array = new int[count1];
   //     int count2 = 0;
   //     for (int i = 0; i < a.length; i++) {
   //         for (int j = 0; j < a.length; j++) {
   //             if (i > j && i > a.length - 1 - j) {
   //                 array[count1++] = a[i][j];
   //             }
   //         }
   //     }}
//
//
   //     //public static int  countunik(int[][] a){
   //     //    int count = 0;
   //     //    for(int i = 0; i<a.length; i++){
   //     //        int d = 0;
   //     //        for (int j=0; j<a.length; j++) {
   //     //            d= a[i][j]; // берем 1 элемент массива и далее перебираем весь массив на повторения
   //     //            for (int k=0; k<a.length; k++) {
   //     //                for(int g=0; g<a.length; g++){
   //     //                    if(d!=a[k][g]) {
   //     //                        count++; // на уникальный элемент ставим счетчик
   //     //                    }
   //     //                }
   //     //            }
//
   //     //        }
//
   //     //    }
   //     //    System.out.println("Количество уникальный элементов массива" + count);
   //     //    return  count;
//
   //     //}

    }
